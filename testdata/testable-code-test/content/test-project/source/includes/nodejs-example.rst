Node.js Include Example
-----------------------

This is an included file with Node.js code.

.. code-block:: javascript

   // Included Node.js code
   const cursor = collection.find();
   await cursor.forEach(doc => console.log(doc));

